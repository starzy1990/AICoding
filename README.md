运行环境

python 3.13

# 安装依赖

```shell
pip install deepagents fastapi uvicorn langchain-openai langgraph-checkpoint-mysql python-dotenv requests
```

sandbox 状态后端依赖

```shell
pip install opensandbox-code-interpreter
```

